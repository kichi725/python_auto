# python_auto
# 環境設定
Jupyter Notebookでの設定

## YouTube DLのインストール
`pip install --upgrade youtube-dl`

再起動必要
## moviepyのインストール
`pip install moviepy`

再起動必要
## インポート
`import youtube_dl`

`from moviepy.editor import *`
